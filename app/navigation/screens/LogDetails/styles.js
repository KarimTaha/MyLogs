import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  pageTitle: {
    fontSize: 35,
    fontWeight: 'bold',
    marginBottom: 20,
    margin: 20,
  },
});

export default styles;
